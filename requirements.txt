# Resource Monitor - Python dependencies
# Install with:
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt

# GUI
PySide6>=6.7,<7

# System monitoring
psutil>=6.0,<8

# Real-time graphs
pyqtgraph>=0.13,<1

# Charts / static exports
matplotlib>=3.9,<4
numpy>=1.26,<3

# NVIDIA GPU telemetry (optional, required for NVIDIA GPU metrics)
nvidia-ml-py>=12.0,<14

# Optional Windows sensor support for CPU temperature detection

# This project also supports reading CPU package temperature from a
# bundled LibreHardwareMonitor executable when available.
# Manual setup (Windows):
#   1. Download LibreHardwareMonitor from:
     https://github.com/LibreHardwareMonitor/LibreHardwareMonitor/releases
#   2. Extract the files and place the executable at:
 #     .tools/LibreHardwareMonitor/LibreHardwareMonitor.exe
#   3. Start the app; it will use that local sensor service automatically.
#
# If the LibreHardwareMonitor executable is missing, the app falls back to:
#   - psutil sensor readings
#   - Windows WMI / PowerShell thermal sensor queries
#   - CPU temperature checks exposed by the OS
#
# No additional Python package is required for the current implementation.
# The app uses Windows built-in sensor APIs and the local LHM binary.
