"""App-wide constants."""

POLL_INTERVAL_MS = 1000  # how often the GUI pulls a new telemetry sample
HISTORY_LENGTH = 60       # number of samples kept for the live graph (~60s at 1s interval)

APP_NAME = "Resource Monitor"
APP_VERSION = "0.1.0-mvp"
